# Simple Feature Request

Add a command that validates all Devflow Kit workflow artifacts before opening a PR.

The command should fail if any required artifact is missing or empty, and it should print the missing path.
